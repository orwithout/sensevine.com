<!-- vite\jsmake_executor\example.html.nunjucks -->
<html>
<head>
  <title>{{ title }}</title>
</head>
<body>
  <h1>{{ title }}</h1>
  <p>{{ getMessage() }}</p>
  <ul>
  {% for item in items %}
    <li>{{ item.name }} - {{ formatPrice(item.price, discount) }}</li>
  {% endfor %}
  </ul>
</body>
</html>
